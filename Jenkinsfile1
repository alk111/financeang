  agent {
    docker {
      image 'docker:latest'
      label 'a1'
      args '-u root:root -v /var/run/docker.sock:/var/run/docker.sock'
    }
  }
  
  stages {
    stage('Build Docker Image') {
      steps {
        sh 'docker build -t my-image .'
      }
    }
    
    stage('Push to ECR') {
      steps {
        withCredentials([[
          credentialsId: 'aws-ecr-creds',
          accessKeyVariable: 'AKIAWOHMJNDAY4LN6UHP',
          secretKeyVariable: '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'
        ]]) {
          sh '''
            # Log in to ECR
            $(aws ecr get-login --no-include-email)

            # Tag the Docker image with the ECR repository URI
            docker tag my-image public.ecr.aws/m2o2j6d0/jenkinsimg/my-image

            # Push the Docker image to ECR
            docker push public.ecr.aws/m2o2j6d0/jenkinsimg/my-image
          '''
        }
      }
    }
  }
}
